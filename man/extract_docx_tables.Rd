% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reports_helpers.R
\name{extract_docx_tables}
\alias{extract_docx_tables}
\title{Extract tables from a Word (.docx) document}
\usage{
extract_docx_tables(path)
}
\arguments{
\item{path}{Path to a \code{.docx} file.}
}
\value{
A list of data frames. Each list element corresponds to one table in the
document (in the order they appear). The first row of each table is used as
the column names.
}
\description{
\code{extract_docx_tables()} reads a Word document and extracts all tables as a
list of data frames, one per table, in document order. Internally, it uses
\code{officer::docx_summary()} to access Word content at the cell level, splits
tables by \code{doc_index}, and reshapes each table to a rectangular structure.
}
\details{
\code{officer::docx_summary()} returns table content as individual cells. Filtering
to \code{content_type == "table cell"} returns cells from all tables in the
document, so multi-table documents must be split by table element (using
\code{doc_index}) before reshaping.

The function assumes that each table has a header row. If a table does not
contain a header row, the first data row will be promoted to column names.
}
\examples{
\dontrun{
tables <- extract_docx_tables("data/quarterly_sales_update.docx")
key_metrics <- tables[[1]]
regional_breakdown <- tables[[2]]
}

}
