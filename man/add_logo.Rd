% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_helpers.R
\name{add_logo}
\alias{add_logo}
\title{Add a logo to a ggplot outside the panel area}
\usage{
add_logo(
  p,
  logo_path,
  position = c("bottom-right", "bottom-left", "top-right", "top-left"),
  width_frac = 0.2,
  offset_x = 0.02,
  offset_y = 0.12,
  margin = ggplot2::margin(1, 1, 3, 1, "lines")
)
}
\arguments{
\item{p}{A ggplot object.}

\item{logo_path}{Character. The file path to a PNG image.}

\item{position}{Character. One of "bottom-right", "bottom-left", "top-right", or "top-left".}

\item{width_frac}{Numeric. The width of the logo as a fraction of the plot width (0 to 1).}

\item{offset_x}{Numeric. Horizontal distance from the panel edge as a fraction of plot width.}

\item{offset_y}{Numeric. Vertical distance from the panel edge as a fraction of plot height.}

\item{margin}{A \code{ggplot2::margin()} object to ensure the logo isn't cut off by the device edge.}
}
\value{
A ggplot2 object with the logo added as an annotation.
}
\description{
This function adds a PNG logo to a ggplot. It uses panel ranges to calculate
coordinates, allowing the logo to be placed in the margins. It automatically
maintains the logo's aspect ratio.
}
\examples{
\dontrun{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point()
add_logo(p, "logo.png", position = "bottom-right")
}
}
